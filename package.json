{
  "name": "quartermaster-app",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.2.0"
  },
  "lint-staged": {
    "frontend/src/**/*.{ts,tsx}": [
      "eslint --fix"
    ],
    "backend/**/*.go": [
      "gofmt -s -w"
    ]
  }
}
